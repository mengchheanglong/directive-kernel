# Runtime Follow-up

This folder is the default Runtime opener surface when a consuming project uses repo-local artifact persistence.

Directive Kernel does not ship historical follow-up records.
Hosts or operators may create follow-up records here when Runtime work is intentionally opened.
