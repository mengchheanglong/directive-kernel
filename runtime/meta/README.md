# Runtime Meta

This folder holds Runtime-wide metadata that still matters in the reusable kernel.

Contents:
- `IMPORT_SOURCE_POLICY.json` - import eligibility policy
- `PROMOTION_PROFILES.json` - empty baseline promotion profile catalog for the consuming project to extend when it owns the required checks and proof surfaces
