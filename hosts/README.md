# Hosts

Hosts are thin runtime/API/UI shells around Directive Kernel.

Current reference hosts:
- `hosts/standalone-host/`
- `hosts/web-host/`

Integration starter surfaces:
- `hosts/integration-kit/`

Host rule:
- keep the host thin
- keep goal resolution in the consuming project
- keep lane vocabulary and workflow ownership in the kernel
