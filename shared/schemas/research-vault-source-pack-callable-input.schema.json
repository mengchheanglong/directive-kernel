{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "shared/schemas/research-vault-source-pack-callable-input.schema.json",
  "title": "Research Vault Source Pack Callable Input",
  "description": "Input for packaging research vault sources into consumable bundles for the kernel intake pipeline.",
  "type": "object",
  "required": ["query"],
  "properties": {
    "query": {
      "type": "string",
      "description": "Search query for source selection"
    },
    "includeEvidence": {
      "type": "boolean",
      "description": "Whether to include evidence snippets in the pack"
    },
    "maxItems": {
      "type": "integer",
      "description": "Maximum number of items to include in the pack",
      "minimum": 1
    }
  },
  "additionalProperties": false
}
